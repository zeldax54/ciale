    {% extends 'gemaBundle::layout.html.twig' %}

    {% block gema_page -%}
    <h2>TipoRaza</h2>

    <div class="table-responsive">
        <table class="record_properties table table-hover">
            <thead>
                <tr>                                                                <th>Tipo</th>
                        
                    <th colspan="3">Acciones</th>
                </tr>
            </thead>
            <tbody>
                <tr>                                        
                        <td>{{ entity.tipo }}</td>
                    
    <td><a href="{{ path('admin_tiporaza_show', { 'id': entity.id }) }}"><i class="fa fa-eye"></i> Mostrar</a></td>
    <td><a href="{{ path('admin_tiporaza_edit', { 'id': entity.id }) }}"><i class="fa fa-edit"></i> Editar</a></td>
    <td><a href="{{ path('admin_tiporaza_delete', { 'id': entity.id }) }}"><i class="fa fa-minus-circle"></i> Eliminar</a></td>

                </tr>
            </tbody>
        </table>
    </div>


    <ul class="pager">
        <li class="next"><a href="{{path('admin_tiporaza')}}"><i class="fa fa-list"></i>
                Listado de TipoRaza
            </a>
        </li>
    </ul>


    {% endblock %}
