    {% extends 'gemaBundle::layout.html.twig' %}

    {% block gema_page -%}
    <h2>Metodopago</h2>

    <div class="table-responsive">
        <table class="record_properties table table-hover">
            <thead>
                <tr>                                                                <th>Dosismin</th>
                                                                    <th>Dosismax</th>
                                                                    <th>Tipo</th>
                                                                    <th>Descuentoporc</th>
                                                                    <th>Descrip1</th>
                                                                    <th>Descrip2</th>
                        
                    <th colspan="3">Acciones</th>
                </tr>
            </thead>
            <tbody>
                <tr>                                        
                        <td>{{ entity.dosismin }}</td>
                                        
                        <td>{{ entity.dosismax }}</td>
                                        
                        <td>{{ entity.tipo }}</td>
                                        
                        <td>{{ entity.descuentoporc }}</td>
                                        
                        <td>{{ entity.descrip1 }}</td>
                                        
                        <td>{{ entity.descrip2 }}</td>
                    
    <td><a href="{{ path('metodopago_show', { 'id': entity.id }) }}"><i class="fa fa-eye"></i> Mostrar</a></td>
    <td><a href="{{ path('metodopago_edit', { 'id': entity.id }) }}"><i class="fa fa-edit"></i> Editar</a></td>
    <td><a href="{{ path('metodopago_delete', { 'id': entity.id }) }}"><i class="fa fa-minus-circle"></i> Eliminar</a></td>

                </tr>
            </tbody>
        </table>
    </div>


    <ul class="pager">
        <li class="next"><a href="{{path('metodopago')}}"><i class="fa fa-list"></i>
                Listado de Metodopago
            </a>
        </li>
    </ul>


    {% endblock %}
