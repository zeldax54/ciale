    {% extends 'gemaBundle::layout.html.twig' %}

    {% block gema_page -%}
    <h2>Productosprogramas</h2>

    <div class="table-responsive">
        <table class="record_properties table table-hover">
            <thead>
                <tr>                                                                <th>Nombre</th>
                                                                    <th>Introduccion</th>
                                                                    <th>Descripcion</th>
                                                                    <th>Publico</th>
                                                                    <th>Guid</th>
                        
                    <th colspan="3">Acciones</th>
                </tr>
            </thead>
            <tbody>
                <tr>                                        
                        <td>{{ entity.nombre }}</td>
                                        
                        <td>{{ entity.introduccion }}</td>
                                        
                        <td>{{ entity.descripcion }}</td>
                                        
                        <td>{{ entity.publico }}</td>
                                        
                        <td>{{ entity.guid }}</td>
                    
    <td><a href="{{ path('admin_productosprogramas_show', { 'id': entity.id }) }}"><i class="fa fa-eye"></i> Mostrar</a></td>
    <td><a href="{{ path('admin_productosprogramas_edit', { 'id': entity.id }) }}"><i class="fa fa-edit"></i> Editar</a></td>
    <td><a href="{{ path('admin_productosprogramas_delete', { 'id': entity.id }) }}"><i class="fa fa-minus-circle"></i> Eliminar</a></td>

                </tr>
            </tbody>
        </table>
    </div>


    <ul class="pager">
        <li class="next"><a href="{{path('admin_productosprogramas')}}"><i class="fa fa-list"></i>
                Listado de Productosprogramas
            </a>
        </li>
    </ul>


    {% endblock %}
