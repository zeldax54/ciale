    {% extends 'gemaBundle::layout.html.twig' %}

    {% block gema_page -%}
    <h2>Correo</h2>

    <div class="table-responsive">
        <table class="record_properties table table-hover">
            <thead>
                <tr>                                                                <th>Nombre</th>
                                                                    <th>Apellido</th>
                                                                    <th>Direccion</th>
                                                                    <th>Provincia</th>
                                                                    <th>Codigopostal</th>
                                                                    <th>Email</th>
                                                                    <th>Telefono</th>
                                                                    <th>Empresa</th>
                                                                    <th>Razas</th>
                                                                    <th>Consulta</th>
                        
                    <th colspan="3">Acciones</th>
                </tr>
            </thead>
            <tbody>
                <tr>                                        
                        <td>{{ entity.nombre }}</td>
                                        
                        <td>{{ entity.apellido }}</td>
                                        
                        <td>{{ entity.direccion }}</td>
                                        
                        <td>{{ entity.provincia }}</td>
                                        
                        <td>{{ entity.codigopostal }}</td>
                                        
                        <td>{{ entity.email }}</td>
                                        
                        <td>{{ entity.telefono }}</td>
                                        
                        <td>{{ entity.empresa }}</td>
                                        
                        <td>{{ entity.razas }}</td>
                                        
                        <td>{{ entity.consulta }}</td>
                    
    <td><a href="{{ path('admin_correo_show', { 'id': entity.id }) }}"><i class="fa fa-eye"></i> Mostrar</a></td>
    <td><a href="{{ path('admin_correo_edit', { 'id': entity.id }) }}"><i class="fa fa-edit"></i> Editar</a></td>
    <td><a href="{{ path('admin_correo_delete', { 'id': entity.id }) }}"><i class="fa fa-minus-circle"></i> Eliminar</a></td>

                </tr>
            </tbody>
        </table>
    </div>


    <ul class="pager">
        <li class="next"><a href="{{path('admin_correo')}}"><i class="fa fa-list"></i>
                Listado de Correo
            </a>
        </li>
    </ul>


    {% endblock %}
