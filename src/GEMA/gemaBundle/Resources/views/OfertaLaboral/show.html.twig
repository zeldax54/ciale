    {% extends 'gemaBundle::layout.html.twig' %}

    {% block gema_page -%}
    <h2>OfertaLaboral</h2>

    <div class="table-responsive">
        <table class="record_properties table table-hover">
            <thead>
                <tr>                                                                <th>Titulo</th>
                                                                    <th>Activa</th>
                                                                    <th>Area</th>
                        
                    <th colspan="3">Acciones</th>
                </tr>
            </thead>
            <tbody>
                <tr>                                        
                        <td>{{ entity.titulo }}</td>
                                        
                        <td>{{ entity.activa }}</td>
                                        
                        <td>{{ entity.area }}</td>
                    
    <td><a href="{{ path('ofertalaboral_show', { 'id': entity.id }) }}"><i class="fa fa-eye"></i> Mostrar</a></td>
    <td><a href="{{ path('ofertalaboral_edit', { 'id': entity.id }) }}"><i class="fa fa-edit"></i> Editar</a></td>
    <td><a href="{{ path('ofertalaboral_delete', { 'id': entity.id }) }}"><i class="fa fa-minus-circle"></i> Eliminar</a></td>

                </tr>
            </tbody>
        </table>
    </div>


    <ul class="pager">
        <li class="next"><a href="{{path('ofertalaboral')}}"><i class="fa fa-list"></i>
                Listado de OfertaLaboral
            </a>
        </li>
    </ul>


    {% endblock %}
