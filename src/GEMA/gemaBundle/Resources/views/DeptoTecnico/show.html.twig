    {% extends 'gemaBundle::layout.html.twig' %}

    {% block gema_page -%}
    <h2>DeptoTecnico</h2>

    <div class="table-responsive">
        <table class="record_properties table table-hover">
            <thead>
                <tr>                                                                <th>Cargo</th>
                                                                    <th>Nombre</th>
                                                                    <th>Telefono</th>
                                                                    <th>Email</th>
                                                                    <th>Publico</th>
                                                                    <th>Guid</th>
                        
                    <th colspan="3">Acciones</th>
                </tr>
            </thead>
            <tbody>
                <tr>                                        
                        <td>{{ entity.cargo }}</td>
                                        
                        <td>{{ entity.nombre }}</td>
                                        
                        <td>{{ entity.telefono }}</td>
                                        
                        <td>{{ entity.email }}</td>
                                        
                        <td>{{ entity.publico }}</td>
                                        
                        <td>{{ entity.guid }}</td>
                    
    <td><a href="{{ path('admin_deptotecnico_show', { 'id': entity.id }) }}"><i class="fa fa-eye"></i> Mostrar</a></td>
    <td><a href="{{ path('admin_deptotecnico_edit', { 'id': entity.id }) }}"><i class="fa fa-edit"></i> Editar</a></td>
    <td><a href="{{ path('admin_deptotecnico_delete', { 'id': entity.id }) }}"><i class="fa fa-minus-circle"></i> Eliminar</a></td>

                </tr>
            </tbody>
        </table>
    </div>


    <ul class="pager">
        <li class="next"><a href="{{path('admin_deptotecnico')}}"><i class="fa fa-list"></i>
                Listado de DeptoTecnico
            </a>
        </li>
    </ul>


    {% endblock %}
